name: Pycleaner Format Workflow

on:
  push:
    paths:
      - '.github/prompts/**'
      - '.github/skills/**'
      - '.github/policies/**'
      - '.github/agents/**'
      - '.github/workflows/**'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Pycleaner format workflow is valid."
          echo "No formatting jobs are configured yet."
