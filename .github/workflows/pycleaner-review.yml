name: Pycleaner Review Workflow

on:
  push:
    paths:
      - '.github/prompts/**'
      - '.github/skills/**'
      - '.github/policies/**'
      - '.github/agents/**'
      - '.github/workflows/**'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Pycleaner review workflow is valid."
          echo "No review jobs are configured yet."
